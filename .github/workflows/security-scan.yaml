name: Security Scans Disabled

# Temporarily disabled while stabilizing Legion core.
# Re-enable later after safe paths, evidence handling, and dashboard auth are stable.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Security scans disabled
        run: echo "Security scans are temporarily disabled while Legion core is stabilized."
